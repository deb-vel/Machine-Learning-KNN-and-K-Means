text files with source code folder contains a text file for each python file with the source code.

Each question has its own .cmd file to run it called:
runQuestion1.cmd
runQuestion2.cmd
runQuestion3.cmd

For these to work you need to have: 
	a. pip installed, 
	b. path directed to python.exe 
	c. numpy installed (pip install numpy)
	d. matplotlib installed (-mpip install matplotlib)
	e. numpy and matplotlib imported (python import numpy) (python import matplotlib)

Plagiarism declaration form is found in a .jpeg file
Statement of completion is included as a table in the report

	